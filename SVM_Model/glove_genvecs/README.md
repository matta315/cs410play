This directory:
```shell script
$ git clone http://github.com/stanfordnlp/glove
$ cd glove && make
$ ./demo.sh
```

Instruction:
* Prepare text, can be multiple docs per line in `text8`
* Run `./demo.sh`